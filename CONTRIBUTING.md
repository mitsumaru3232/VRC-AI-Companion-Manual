# マニュアルを更新する方へ

このリポジトリは購入者向けの公開マニュアルです。アプリ本体の実装、配布EXE、個人設定、会話履歴、APIキーを追加しないでください。
このページは編集者向けのため、購入者用の目次には含めていません。

## 文章を直す

1. 該当するMarkdownファイルを編集する。画面にあるボタン名を使い、操作と成功したときの状態を説明する。
2. ページを増やしたら `SUMMARY.md` に追加する。画像とページへのリンクは相対パスにする。
3. 対応するアプリ版と公式情報の確認日を確認し、変更点を `CHANGELOG.md` に記録する。
4. Python 3.10以降で `python scripts/check_docs.py` を実行する。
5. 差分と画像を目視確認してからコミットする。

料金・無料枠・モデル名・キー発行画面は変更されるため、記憶で書かずに各ページの公式リンクから再確認してください。
ログイン後のサービス管理画面は、購入者のアカウントで表示が変わります。個人の管理画面の画像を載せるより、操作手順と公式案内へのリンクを優先します。

## アプリ画像を更新する

画像の取得スクリプト `scripts/capture_manual.py` は、非公開のアプリ開発リポジトリで管理しています。
開発環境で次を実行し、出力先にこのマニュアルのチェックアウトを指定します。

```powershell
.venv/Scripts/python.exe -X utf8 scripts/capture_manual.py --output <マニュアルのチェックアウト先>/assets/screenshots
```

実際のQt画面を、一時設定・サンプル会話・空のAPIキーで撮影します。通常の個人設定や会話DBは使わず、通信と音声デバイスへのアクセスを止めています。
画像はアプリの操作画面ですが、接続状態・モデル名・会話は説明用の例です。端末固有のデバイス一覧や実サービスの疎通を示すものではありません。

生成される `assets/screenshots/manifest.json` にアプリ版、画像サイズ、画像のハッシュを記録します。
再取得後は画像を開いて文字の欠け、秘密情報、本文との一致を確認し、本文中の対応バージョンも更新してください。
画面の構造が変わった場合は、非公開側の撮影スクリプトを修正します。

## GitBookに接続する

このリポジトリには `README.md`、`SUMMARY.md`、`.gitbook.yaml` を用意しています。
[GitBook公式のGit Sync説明](https://gitbook.com/docs/docs-as-code/git-sync)に沿って、マニュアル用のSpaceからこのリポジトリの `main` ブランチに接続します。
初回同期は、今回作成したGitHub上の文書をGitBookへ取り込む方向を選びます。

GitHub連携アプリへのアクセス許可は、このマニュアル用リポジトリだけに絞ってください。
同期後は目次、画像、リンクをプレビューで確認し、公開サイトの設定と公開操作を行います。
Git Syncの設定だけで購入者向けサイトの公開が完了したとは限りません。

## 検査の範囲

`check_docs.py` はローカルリンク、目次、画像ハッシュ、ファイル構成、典型的な秘密情報の文字列を検査します。
外部URLの有効性や文章の正しさ、画像に映った内容は自動判定しません。公開前の目視確認は必要です。
